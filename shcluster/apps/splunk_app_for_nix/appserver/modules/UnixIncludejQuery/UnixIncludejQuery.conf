[module]
className = Splunk.Module.UnixIncludejQuery
superClass = Splunk.Module
description = Include jQuery 1.9 in your view by placing this module at the top of the view hierarchy. Instance accessible via UnixjQuery, not jQuery, to avoid conflict with core's version. Also adds required jquery plugins by Modules system like lowpro.
